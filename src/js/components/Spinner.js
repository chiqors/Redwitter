import React from 'react';

const Spinner = () => {
    return (
        <section class="section u-centered">
            Loading
            <i class="fa fa-spinner fa-spin fa-3x fa-fw" aria-hidden="true"></i>
        </section>
    );
};

export default Spinner;
